# Contributing

感谢你改进这些 Skill。请保持变更小而明确，并说明它解决了什么真实问题。

## 提交前检查

1. 不要提交用户图片、生成结果、临时裁剪、PPTX、访问令牌或本机绝对路径。
2. 保持 `SKILL.md` 的触发描述准确，避免让 Skill 捕获无关请求。
3. 新增参考文档时，从 `SKILL.md` 明确链接并说明何时需要阅读。
4. 新增或修改脚本时，提供实际运行结果，并避免覆盖用户源文件。
5. 使用 Skill Creator 的 `quick_validate.py` 校验两个 Skill。
6. 确认 README 和操作指南与实际行为一致。

## Pull Request

PR 描述应包含：

- 变更目的；
- 受影响的 Skill；
- 验证方式；
- 如涉及视觉输出，说明核对了哪些可观察不变量。
